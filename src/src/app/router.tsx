// 🧭 GTS Platform - Simple Router (No External Imports)
// ✅ Minimalist version to avoid build errors

'use client'

import React from 'react'

export function AppRouter() {
  return (
    <div className="p-8">
      <h2 className="text-xl font-semibold mb-4">App Router</h2>
      <p className="text-gray-400">Simple router without external dependencies</p>
    </div>
  )
}